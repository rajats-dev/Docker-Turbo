{
  "name": "ws",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --env-file=../../.env run index.ts",
    "start": "bun run db:generate && bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "db": "*"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
